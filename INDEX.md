# Repo map

- SPEC.md — decisions (D-NN) + phase contracts
- src/resgraph/schema.py — D2 update-message schema (pydantic)
- tests/ — schema + (per chapter) component tests
- compose.yaml — local stores (grows per chapter)
- .github/workflows/ci.yml — lint + tests (+gates later)
